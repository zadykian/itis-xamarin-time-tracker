<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:TimeTracker.Views" mc:Ignorable="d"
             NavigationPage.HasNavigationBar="False"
             xmlns:buttons="clr-namespace:TimeTracker.Views.Buttons"
             xmlns:pageModels="clr-namespace:TimeTracker.PageModels;assembly=TimeTracker.Core.Xamarin"
             x:Class="TimeTracker.Pages.LoginPage"
             x:DataType="pageModels:LoginPageModel">
	<ContentPage.Content>
		<StackLayout WidthRequest="220" HorizontalOptions="Center" VerticalOptions="Center">

			<views:LoginEntryView BindingContext="{Binding UsernameEntryViewModel}" />
			<views:LoginEntryView BindingContext="{Binding PasswordEntryViewModel}" />

			<buttons:BindableButton BindingContext="{Binding CreateAccountButtonViewModel}" />
			<buttons:BindableButton BindingContext="{Binding LogInButtonViewModel}" />

		</StackLayout>
	</ContentPage.Content>
</ContentPage>